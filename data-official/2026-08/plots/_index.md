# `data-official/2026-08/plots/` — August cycle charts

Every plot here is generated by a cell in `../august_canonical_v2026-07-28.ipynb` (the `desktop_*` /
`mobile_*` files) or `../desktop_adjustment_ladder.ipynb` (the `ladder_*` files), and saved from that
cell. **Do not add plots produced by standalone scripts** — if a chart is worth keeping it belongs in a
notebook cell the reader can re-run.

## Adjustment-ladder diagnostics (`../desktop_adjustment_ladder.ipynb`)

| file | shows |
|---|---|
| `ladder_00_raw.png` | Desktop with **no** adjustments at all — the model's own summer shape, continuous across the seam |
| `ladder_01_overlays.png` | The baked tailwinds added one at a time (none → `l` → `o` → `l`+`o`), full year + seam zoom |
| `ladder_01b_overlay_effects.png` | Each overlay's effect as a curve, plus whether the two are additive (they are not) |
| `ladder_02_headwind_conventions.png` | **The plot that explains the seam drop** — headwind off vs as-applied vs seam-anchored |
| `ladder_02b_headwind_ramps.png` | The two headwind ramps themselves; both terminate on the same Dec-15 anchor |
| `ladder_03_full.png` | The whole ladder accumulated, ending on the published curve |

## Canonical forecast charts (`../august_canonical_v2026-07-28.ipynb`)

| file | produced by | shows |
|---|---|---|
| `desktop_current_vs_july.png` | `[plot-desktop]` | Desktop DAU 28d-MA: actuals, July delivered, August current build, Dec-15 markers |
| `desktop_current_only.png` | `[plot-desktop-current-only]` | Same without the prior line |
| `mobile_august_vs_july.png` | `[plot-mobile]` | Mobile DAU 28d-MA, same three series |
| `mobile_august_only.png` | `[plot-mobile-current-only]` | Same without the prior line |
| `mobile_ex_iran_august_vs_july.png` | `[plot-mobile-ex-iran]` | Mobile ex-Iran (ALL − IR), which strips the post-shutdown reconnection bounce |

Desktop filenames are deliberately **cap-agnostic** (`desktop_current_*`). The LOL ceiling and the
headwind anchor are both being iterated this cycle, and encoding the variant in the filename churned the
tracked plot set on every rebuild; the chart title carries the variant instead. Earlier `*_baseline_*` and
`*_lol165_*` files were superseded 1:1 and removed. The mobile charts are unchanged in content across all
variants in shape — `l` is desktop-only and mobile's headwind amplitude never moved; the ramp
re-anchoring shifted mobile's near-term curve by ~12K, which is invisible at that scale.

## Reading the charts

- **The seam step is GONE as of 2026-07-29 — and here is what it was.** The Win10 headwind used to ramp
  from 2026-04-01 while only being *applied* from the seam forward, so it switched on at 118/258 = 45.7% of
  full value (−569,419) in a single day. The raw model output is continuous across the seam (+5,157), so
  that step was **100.9%** of the −564,262 drop visible in earlier charts. `headwind.json` now ramps from
  the seam instead: same Dec-15 anchor, zero at the seam, no step. June's and July's charts still show the
  old behaviour at their own seams. Measurement and both conventions:
  `../desktop_adjustment_ladder.ipynb`; see also `research/ma-seam-turbulence/`.
- **The gold low/baseline/stretch markers are NOT August targets.** They are the June-cycle aspirational
  benchmarks, reused unchanged in July and again here purely so the vertical scale stays comparable
  across the three cycles' charts. No August target has been set.
- Y-axis ticks are formatted to two decimal places of a million so adjacent ticks stay distinguishable
  (a whole-million formatter would render 48.52M and 48.59M as the same "49M").

**Where new files go:** charts from this cycle's notebooks. Cross-month diagnostic plots belong under
`research/{topic}/plots/`.
