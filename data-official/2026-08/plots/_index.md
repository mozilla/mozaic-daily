# `data-official/2026-08/plots/` — August cycle charts

Every plot here is generated by a cell in one of four cycle notebooks and saved from that cell:

| prefix | producer |
|---|---|
| `desktop_*`, `mobile_august_*`, `mobile_ex_iran_*`, `mobile_organic_paid_*` | `../august_canonical_v2026-07-28.ipynb` |
| `ladder_*` | `../desktop_adjustment_ladder.ipynb` |
| `seam_fix_*` | `../seam_fix_before_after.ipynb` |
| `marketing_*` | `../marketing/august_marketing_lift.ipynb` |
| `mobile_kpi_cake.png` | `../mobile_kpi_cake.ipynb` |
| `kpi_sheet_*` | `../kpi_sheet/plot_future_lines.ipynb` |

**Do not add plots produced by standalone scripts** — if a chart is worth keeping it belongs in a
notebook cell the reader can re-run. Only the first two groups feed published numbers or the handoff
bundle; `marketing_*` are curve-selection diagnostics.

## KPI-workbook draft render check (`../kpi_sheet/plot_future_lines.ipynb`)

| file | shows |
|---|---|
| `kpi_sheet_future_desktop.png` | The two `FUTURE` (draft August) lines exactly as the KPI tab will draw them, desktop |
| `kpi_sheet_future_mobile.png` | Same, mobile |

These read the **generated tab rows** (`../kpi_sheet/official_forecast_data.FUTURE_ONLY.csv`), not
the parquets or `../csv/`, so they are a render check on what gets pasted into the workbook rather
than another view of the forecast. They feed no published number.

The orange dash-dot marks the JUN→JUL handoff gap at 2026-07-05, which breaks the prior line between
those two vintages rather than letting it connect across their **+895,841** desktop / **+414,383**
mobile level disagreement. (These were first cut without that gap, showing the disagreement as a
near-vertical spike; the gap was added the same day.) Contrast the grey seam line at 2026-08-02,
where the step is between two *separate* series and so needs no gap. See `../kpi_sheet/_index.md`.

## Adjustment-ladder diagnostics (`../desktop_adjustment_ladder.ipynb`)

| file | shows |
|---|---|
| `ladder_00_raw.png` | Desktop with **no** adjustments at all — the model's own summer shape, continuous across the seam |
| `ladder_01_overlays.png` | The baked tailwinds added one at a time (none → `l` → `o` → `l`+`o`), full year + seam zoom |
| `ladder_01b_overlay_effects.png` | Each overlay's effect as a curve, plus whether the two are additive (they are not) |
| `ladder_02_headwind_conventions.png` | **The plot that explains the seam drop** — headwind off vs as-applied vs seam-anchored |
| `ladder_02b_headwind_ramps.png` | The two headwind ramps themselves; both terminate on the same Dec-15 anchor |
| `ladder_03_full.png` | The whole ladder accumulated, ending on the published curve |

## Canonical forecast charts (`../august_canonical_v2026-07-28.ipynb`)

| file | produced by | shows |
|---|---|---|
| `desktop_current_vs_july.png` | `[plot-desktop]` | Desktop DAU 28d-MA: actuals, July delivered, August current build, Dec-15 markers |
| `desktop_current_only.png` | `[plot-desktop-current-only]` | Same without the prior line |
| `mobile_august_vs_july.png` | `[plot-mobile]` | Mobile DAU 28d-MA, same three series |
| `mobile_august_only.png` | `[plot-mobile-current-only]` | Same without the prior line |
| `mobile_ex_iran_august_vs_july.png` | `[plot-mobile-ex-iran]` | Mobile ex-Iran (ALL − IR), which strips the post-shutdown reconnection bounce |
| `desktop_current_vs_july_with_2025.png` | `[plot-desktop-vs-july-with-2025]` | `desktop_current_vs_july` **plus a faint grey 2025-actuals reference line** |
| `desktop_current_only_with_2025.png` | `[plot-desktop-current-only-with-2025]` | August desktop alone against the 2025 reference — the cleanest year-over-year read |
| `mobile_august_vs_july_with_2025.png` | `[plot-mobile-vs-july-with-2025]` | `mobile_august_vs_july` plus the 2025 reference |
| `mobile_august_only_with_2025.png` | `[plot-mobile-current-only-with-2025]` | August mobile alone against the 2025 reference |
| `desktop_headwind_on_vs_off.png` | `[plot-desktop-headwind-on-off]` | August desktop with the Win10 headwind `h` applied (published) vs removed (counterfactual), shaded gap = the ramp. Reads the published CSV + its `WIN10_HEADWIND_REMOVED` twin, so it cannot disagree with the handoff files |

### The `*_with_2025` variants

These are **additional** charts, not replacements. `render_desktop_plot` / `render_mobile_plot` gained an
optional `show_2025=False` flag, and the 2025 line is drawn only inside `if show_2025:`, so the four
originals are unaffected by the change. (They *were* rewritten when the notebook was re-run on
2026-07-30, because `[bq-actuals]` advanced the actuals line by one day, 2026-07-27 → 2026-07-28. No
forecast number moved: Dec-15, the attribution ledger, and the prior-curve reproduction check are all
identical. The one table change is the Actuals row's "Aug trough min" going `N/A` → `47,108,607`, which is
a single observed day inside the 07-28..10-15 window rather than a meaningful trough.)

The split into separate charts exists because the 2025 line is drawn at **true 2025 levels, not rebased**,
and the two years' ranges differ enough to cost real vertical resolution:

| | 2025 range (28d MA) | 2026 display range | Dec-15: current vs 2025 |
|---|---|---|---|
| Desktop | 47.06M – 53.95M | 44.8M – 50.0M | **−3,047,795** (2025 was higher) |
| Mobile | 13.43M – 15.71M | 15.2M – 18.0M | **+2,210,843** (2025 was lower) |

(2025 Dec-15 references: desktop **51,846,238**, mobile **15,690,719** — unchanged, they are actuals. Both gaps are against the
**published** current curves, so they moved when the g01 retune, the mobile re-lock and the `t` tailwind
landed — recompute them alongside any rebuild rather than carrying them forward.)

Mobile is the binding case — the two years barely overlap, so a combined axis spans ~4.5M instead of
~2.7M and visually flattens the curve the headline chart exists to show. Keeping the reference on its own
chart preserves both readings. (Desktop's Dec-15 gap is against the **headwind-adjusted** current curve;
against the raw model output it is −1,827,795, the difference being the −1,220,000 `h` anchor. Mobile's
gap is likewise against the published curve, which *includes* the +276,000 `t` tailwind — against the
pre-tailwind curve it is +1,934,843.)

The 2025 series is read from the **`training` rows already in the forecast parquets**, not a fresh
BigQuery query — verified equal to raw actuals day-for-day (61/61 exact, 0 DAU difference, March and
September 2025 probes, both platforms) by `scripts/verify_training_rows_are_actuals.py`. Every
training-row modification in the pipeline (Iran fill, `l`, `o`, `m`) is confined to 2026. Re-querying BQ
for 2025 would add ~1TB of scan (~$5) to every notebook run for an identical series.

Alignment is by **calendar date** (2025-12-15 drawn at the 2026-12-15 position), so holidays and seasonal
turns land in the right place. Day-of-week drifts by one day, which costs nothing on a 28-day MA that has
already averaged the weekly cycle out.

Desktop filenames are deliberately **cap-agnostic** (`desktop_current_*`). The LOL ceiling and the
headwind anchor were both iterated this cycle, and encoding the variant in the filename churned the
tracked plot set on every rebuild; the chart title carries the variant instead. Earlier per-variant files
were superseded 1:1 and removed, as were the superseded LOL curve plots under
`../launch_on_login/plots/` (2026-07-30 — only the active curve's plot remains). The mobile charts are unchanged in content across all
variants in shape — `l` is desktop-only and mobile's headwind amplitude never moved; the ramp
re-anchoring shifted mobile's near-term curve by ~12K, which is invisible at that scale.

## Seam-fix verification (`../seam_fix_before_after.ipynb`)

| file | shows |
|---|---|
| `seam_fix_before_after_ma.png` | **Fix A verification.** Desktop and mobile 28d-MA across the seam, published (pre-fix) vs fixed, with the plain 28d-MA drawn as the honest reference. That notebook is frozen with its outputs as proof of the choice |
| `seam_fix_before_after_daily.png` | The same comparison at daily resolution — the +5,921,427 day-1 rebuilt-value spike disappearing |

## Mobile paid/organic (`../august_canonical_v2026-07-28.ipynb`)

| file | produced by | shows |
|---|---|---|
| `mobile_organic_paid_decomposition.png` | `[plot-mobile-organic-paid]` | The `p` split as published: measured organic vs stacked paid level, with the actuals→forecast seam |

## Mobile KPI cumulative decomposition (`../mobile_kpi_cake.ipynb`)

| file | produced by | shows |
|---|---|---|
| `mobile_kpi_cake.png` | `[plot-cake]` | **The "cake" chart.** Three *cumulative* lines Jan→Dec 2026: organic DAU, + paid marketing, + headwind `h` & tailwind `t`. The top line **is** the published mobile KPI, so the vertical gaps are the contributions themselves. Solid to the seam (actuals), dashed after. Still carries the `DRAFT` watermark — see the note below. **Y-axis is truncated** — the organic band is clipped at the axis floor, not zero, and the chart says so |

Why this is a separate notebook from `mobile_organic_paid_decomposition.png`: that chart splits the
published total into organic/paid/tailwind bands where **organic absorbs the headwind**. The cake chart
instead gives `h` + `t` their own combined band, so the model's output (layers 1–2) and the judgement
layer (layer 3) are visually separable. Both reconstruct the published headline exactly and both assert it.

## Marketing-curve selection (`../marketing/august_marketing_lift.ipynb`)

Produced while choosing the August paid curve that `p` consumes. **Diagnostics, not canonical
output** — they feed no published number and are not part of the handoff bundle. They are kept here
rather than under `research/` because the choice was month-scoped.

| file | shows |
|---|---|
| `marketing_source_uac_meta.png` | The UAC + Meta spend/install source series the candidate curves were built from |
| `marketing_basis_decomposition.png` | The candidate lift curves decomposed to their basis components |
| `marketing_method_d_candidates.png` | The Method-D candidate family side by side |
| `marketing_july_vs_august_curves.png` | July's shipped lift curve against the August candidates |
| `marketing_testapply_kpi.png` | Each candidate's KPI effect when test-applied — the measurement that exposed the retired `m` overlay absorbing 58% of any curve change |

## Reading the charts

- **Both seam steps are now fixed (2026-07-29).** The headwind one went first (re-anchoring the ramp to
  the seam). The second was a **+102,595** upward display artifact in `reconstruct_matched_daily`, whose
  7-day centered trend, computed on the forecast only, degenerated to a weekday-only window at the seam.
  `Fix A` divides by the forecast's own day-of-week profile before smoothing; the residual distortion on
  this build is **+102 DAU**, and the curve now steps −107,445 against a plain 28d-MA step of −107,547.
  **The curve therefore leaves the actuals heading slightly DOWN, and that is correct** — the old upward
  step was masking a real decline. Aug-25 and Dec-15 were byte-identical before and after. A near-zero
  seam step is not a quality signal: the pre-retune build's +5,157 was two errors cancelling. See
  `research/ma-seam-turbulence/LOG.md` § Fix A and `../seam_fix_before_after.ipynb`. What the headwind step WAS: The Win10 headwind used to ramp
  from 2026-04-01 while only being *applied* from the seam forward, so it switched on at 118/258 = 45.7% of
  full value (−569,419) in a single day. The raw model output is continuous across the seam (+5,157), so
  that step was **100.9%** of the −564,262 drop visible in earlier charts. `headwind.json` now ramps from
  the seam instead: same Dec-15 anchor, zero at the seam, no step. June's and July's charts still show the
  old behaviour at their own seams. Measurement and both conventions:
  `../desktop_adjustment_ladder.ipynb`; see also `research/ma-seam-turbulence/`.
- **The gold low/baseline/stretch markers are NOT August targets.** They are the June-cycle aspirational
  benchmarks, reused unchanged in July and again here purely so the vertical scale stays comparable
  across the three cycles' charts. No August target has been set.
- Y-axis ticks are formatted to two decimal places of a million so adjacent ticks stay distinguishable
  (a whole-million formatter would render 48.52M and 48.59M as the same "49M").

## The mobile `DRAFT` watermark — currently OFF, and wired to come back

Every mobile chart carried a large diagonal `DRAFT` watermark while mobile's discretionary `t`
overlay was undecided. **Mobile was signed off on 2026-08-03 and the watermark was switched off on
all five mobile charts.** Desktop never carried one — it has no equivalent undecided overlay.

**It is switched off, not removed.** The `add_draft_watermark()` helper and every one of its call
sites are deliberately left in place, because reapplying it is expected. Each producing notebook
carries one flag:

| notebook | flag lives in | call sites |
|---|---|--:|
| `../august_canonical_v2026-07-28.ipynb` | `[helpers]` | 3 |
| `../mobile_kpi_cake.ipynb` | `[setup]` | 1 |

```python
SHOW_DRAFT_WATERMARK = False   # -> True to re-watermark every mobile chart in that notebook
```

The helper no-ops when the flag is `False`, so **flip the flag and re-execute — never re-add
per-chart calls.** The two copies are kept deliberately identical so the canonical and cake chart
sets can never disagree about whether they are watermarked.

**Switching it off did not retire the caveat.** Published mobile Dec-15 is still 1.67% `t`, and the
last +23,000 of that was sized to land the total within 1,000 DAU of July's figure. While the
watermark is off, that travels only in text: `../tailwind/_index.md`, the `[mobile-dec15]`
decomposition, `../csv/README.md`.

**Where new files go:** charts from this cycle's notebooks. Cross-month diagnostic plots belong under
`research/{topic}/plots/`.
